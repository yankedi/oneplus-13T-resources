# 贡献指南

本仓库的目标是保留可追溯、可复现的 OnePlus 13T 技术记录。补充资料时，请优先提交小而明确的变更。

> 本轮主动来源收集已经结束。维护优先级依次是：纠正既有记录、补充可复现实机结果、更新已登记上游的冻结状态。仅提供新的论坛链接不会自动进入主分支；重新扩充来源前需先明确恢复收集范围。

## 可接受的内容

- 官方文档、公开源码仓库或可定位到具体版本的上游资料。
- 带日期、设备型号、系统版本、槽位和测试步骤的本机复现结果。
- 已脱敏的最小日志片段，以及从原始证据推导出的结论。
- 对已有结论的纠错，附可复核证据和受影响页面。

## 不接受的内容

- 酷安内容默认不接收；只有 `data/source-index.yaml` 已明确列出的账号与主题例外可以作为待验证线索，且不得扩展引用范围。
- 完整 OTA、刷机包、分区镜像、原始备份、keybox、账号令牌或设备唯一标识。
- 无法区分事实、推断和待验证项的“听说可用”。
- 用不同固件代际的 vendor/firmware 直接覆盖已验证基线的建议。

## 记录格式

每条技术结论至少写明：

1. **对象**：型号、代号、系统/固件版本和相关槽位。
2. **时间**：资料核对或设备测试日期。
3. **操作**：做了什么；若发生写入，写明目标分区或文件。
4. **结果**：成功、失败、部分成功或未验证。
5. **证据**：公开 URL、提交 SHA、校验值或最小脱敏日志。
6. **边界**：没有测试什么，不能由当前证据推出什么。

机器可读来源同时更新 [`data/source-index.yaml`](data/source-index.yaml)；功能差异同时更新 [`data/feature-matrix.tsv`](data/feature-matrix.tsv)。

## 提交流程

- 新来源或测试结果优先通过对应 Issue 表单提交。
- 修改文档时保持一个提交只处理一个主题。
- Pull Request 中列出证据、风险、验证方法和隐私检查结果。
- 对 `NEEDS_DEVICE_TEST` 项，不要在未实测前改成 `PASS`。
